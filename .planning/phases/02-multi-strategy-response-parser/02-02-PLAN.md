---
phase: 02-multi-strategy-response-parser
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified: [src/perplexity_web_mcp/api/strategies/key_value.py, tests/test_key_value_strategy.py]
autonomous: true

must_haves:
  truths:
    - "Parser extracts KEY: value patterns from text"
    - "Parser handles multiple key-value pairs"
    - "Parser normalizes keys to lowercase"
  artifacts:
    - path: "src/perplexity_web_mcp/api/strategies/key_value.py"
      provides: "Key-value pattern extraction strategy"
      exports: ["extract_key_value_patterns"]
    - path: "tests/test_key_value_strategy.py"
      provides: "Tests for key-value strategy"
      min_lines: 40
  key_links:
    - from: "extract_key_value_patterns"
      to: "re.match"
      via: "Regex pattern matching"
      pattern: "re\\.match.*KEY_VALUE_PATTERN"
---

<objective>
Build key-value strategy that extracts simple command patterns like "SEARCH: query"

Purpose: Enable fallback parsing for simpler tool invocation formats
Output: Working key-value parser with comprehensive tests
</objective>

<execution_context>
@/Users/jbendavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jbendavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-strategy-response-parser/02-RESEARCH.md
</context>

<feature>
  <name>Key-Value Pattern Extraction</name>
  <files>src/perplexity_web_mcp/api/strategies/key_value.py, tests/test_key_value_strategy.py</files>
  <behavior>
    Extract KEY: value patterns from text

    Cases:
    - "SEARCH: weather today" -> [{'tool': 'search', 'input': 'weather today'}]
    - "READ_FILE: /path/to/file.txt" -> [{'tool': 'read_file', 'input': '/path/to/file.txt'}]
    - "CALCULATE: 2 + 2" -> [{'tool': 'calculate', 'input': '2 + 2'}]
    - "SEARCH: first query\nREAD: second file" -> [{'tool': 'search', ...}, {'tool': 'read', ...}]
    - "no patterns here" -> []
    - "Search: lowercase should work" -> [{'tool': 'search', 'input': 'lowercase should work'}]
  </behavior>
  <implementation>
    1. Define KEY_VALUE_PATTERN regex: r'^([A-Z_]+):\s*(.+)$'
    2. Split text by newlines
    3. Match each line against pattern
    4. Normalize key to lowercase for tool name
    5. Strip whitespace from value
    6. Return list of tool invocations
    7. Handle case-insensitive matching as fallback
  </implementation>
</feature>

<output>
After completion, create `.planning/phases/02-multi-strategy-response-parser/02-02-SUMMARY.md`
</output>