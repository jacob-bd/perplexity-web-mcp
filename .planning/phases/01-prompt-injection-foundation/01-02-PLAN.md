---
phase: 01-prompt-injection-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/perplexity_web_mcp/api/prompt_builder.py, tests/test_prompt_builder.py]
autonomous: true

must_haves:
  truths:
    - "Tool definitions appear before user message in prompts"
    - "System prompts get distilled and integrated"
    - "Usage instructions tell model how to call functions"
  artifacts:
    - path: "src/perplexity_web_mcp/api/prompt_builder.py"
      provides: "Prompt construction with tool injection"
      min_lines: 60
      exports: ["build_prompt_with_tools", "inject_tools_minimal"]
    - path: "tests/test_prompt_builder.py"
      provides: "Unit tests for prompt building"
      min_lines: 40
  key_links:
    - from: "prompt_builder.py"
      to: "tool_injection.py"
      via: "import and use format functions"
      pattern: "from.*tool_injection import.*format_tool"
    - from: "prompt_builder.py"
      to: "session_manager.py"
      via: "import distill_system_prompt"
      pattern: "from.*session_manager import.*distill_system_prompt"
---

<objective>
Create the prompt builder module that constructs complete prompts with tool definitions injected before the user message.

Purpose: Tools must be positioned correctly in the prompt (before user message) with clear usage instructions for the model to recognize them.

Output: A module that combines system context, tool definitions, and user messages into a complete prompt.
</objective>

<execution_context>
@/Users/jbendavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jbendavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prompt-injection-foundation/01-RESEARCH.md
@.planning/phases/01-prompt-injection-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Prompt Builder Module</name>
  <files>src/perplexity_web_mcp/api/prompt_builder.py</files>
  <action>
  Create a module for building complete prompts with tool injection.

  Implement two main functions:
  1. build_prompt_with_tools(user_message: str, tools: list[dict], system: str = None) -> str
     - Start with distilled system context if provided (use existing distill_system_prompt from session_manager)
     - Add "Available functions you can call:" header
     - Wrap tool definitions in ```python``` markdown code block
     - Use format_tool_as_python from tool_injection module for each tool
     - Add usage instructions: "To use a function, write: functionname(param1='value1', param2='value2')"
     - Add note: "Then I'll execute it and show you the result."
     - Finally append the user's actual message

  2. inject_tools_minimal(tools: list[dict]) -> str
     - Simpler format for testing/fallback
     - List tools as bullet points with name and first line of description
     - Add simple usage instruction
     - Reference pattern from RESEARCH.md lines 208-224

  Import from tool_injection for formatting and session_manager for system prompt distillation.
  Follow the prompt structure pattern from RESEARCH.md lines 99-124.
  </action>
  <verify>python -c "from src.perplexity_web_mcp.api.prompt_builder import build_prompt_with_tools, inject_tools_minimal; print('Import successful')"</verify>
  <done>Module exists with both prompt building functions exported</done>
</task>

<task type="auto">
  <name>Task 2: Add Prompt Builder Tests</name>
  <files>tests/test_prompt_builder.py</files>
  <action>
  Create unit tests for the prompt builder module.

  Test cases:
  1. Prompt with tools and no system message
  2. Prompt with tools and system message
  3. Prompt with empty tools list (should just return user message)
  4. Prompt with multiple tools
  5. Minimal injection format test
  6. Verify tool definitions appear before user message
  7. Verify markdown code block formatting

  Each test should:
  - Create sample tools and messages
  - Call build_prompt_with_tools or inject_tools_minimal
  - Assert tools appear in correct position
  - Assert markdown formatting is correct
  - Assert usage instructions are present

  Use pytest for test structure.
  </action>
  <verify>python -m pytest tests/test_prompt_builder.py -v</verify>
  <done>All tests pass showing prompts built correctly with tools injected</done>
</task>

</tasks>

<verification>
1. Run tests: `python -m pytest tests/test_prompt_builder.py -v`
2. Test full prompt generation with sample tool and message
3. Verify markdown code blocks and positioning
</verification>

<success_criteria>
- Prompt builder module exists and exports required functions
- Tool definitions appear in markdown code blocks before user message
- Usage instructions included in prompt
- System prompt distillation integrated
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-prompt-injection-foundation/01-02-SUMMARY.md`
</output>