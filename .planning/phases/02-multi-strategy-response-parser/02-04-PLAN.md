---
phase: 02-multi-strategy-response-parser
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified: [src/perplexity_web_mcp/api/response_parser.py, src/perplexity_web_mcp/api/strategies/__init__.py, src/perplexity_web_mcp/api/extractors/markdown.py]
autonomous: true

must_haves:
  truths:
    - "Parser tries all strategies in order"
    - "Parser returns first successful result"
    - "Parser handles failures gracefully"
    - "Parser extracts markdown blocks before parsing"
  artifacts:
    - path: "src/perplexity_web_mcp/api/response_parser.py"
      provides: "Main parser orchestrator"
      exports: ["parse_response"]
    - path: "src/perplexity_web_mcp/api/extractors/markdown.py"
      provides: "Markdown block extraction"
      exports: ["extract_python_blocks"]
    - path: "src/perplexity_web_mcp/api/strategies/__init__.py"
      provides: "Strategy exports"
      exports: ["extract_python_calls", "extract_key_value_patterns", "extract_inline_code_mentions"]
  key_links:
    - from: "response_parser.py"
      to: "strategies"
      via: "imports"
      pattern: "from.*strategies.*import"
    - from: "parse_response"
      to: "extract_python_blocks"
      via: "markdown extraction first"
      pattern: "extract_python_blocks.*before.*parse"
---

<objective>
Create response parser orchestrator that applies strategies in fallback chain

Purpose: Coordinate multiple parsing strategies to maximize tool call extraction
Output: Main parser that tries AST -> key-value -> inline code strategies
</objective>

<execution_context>
@/Users/jbendavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jbendavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-strategy-response-parser/02-RESEARCH.md
@.planning/phases/02-multi-strategy-response-parser/02-01-SUMMARY.md
@.planning/phases/02-multi-strategy-response-parser/02-02-SUMMARY.md
@.planning/phases/02-multi-strategy-response-parser/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create markdown extractor utility</name>
  <files>src/perplexity_web_mcp/api/extractors/markdown.py</files>
  <action>
    Create markdown extraction utilities that extract code blocks before parsing.

    Create extractors/__init__.py first (empty).

    In markdown.py:
    - Install mdextractor: Add to pyproject.toml dependencies
    - Import mdextractor (fallback to regex if not available)
    - Create extract_python_blocks(text: str) -> list[str] that returns Python code blocks
    - Create extract_all_code_blocks(text: str) -> list[dict] that returns all blocks with language tags
    - Handle edge cases: nested blocks, no language tags, mixed content
    - Use mdextractor.extract_md_blocks if available, else regex fallback
  </action>
  <verify>python -c "from src.perplexity_web_mcp.api.extractors.markdown import extract_python_blocks; print(extract_python_blocks('```python\nprint(1)\n```'))"</verify>
  <done>Markdown extraction utilities exist and can extract Python code blocks</done>
</task>

<task type="auto">
  <name>Task 2: Create parser orchestrator with fallback chain</name>
  <files>src/perplexity_web_mcp/api/response_parser.py, src/perplexity_web_mcp/api/strategies/__init__.py</files>
  <action>
    Create the main response parser that coordinates strategies.

    In strategies/__init__.py:
    - Import all three strategies
    - Export extract_python_calls, extract_key_value_patterns, extract_inline_code_mentions

    In response_parser.py:
    - Import logging, create logger
    - Import strategies and markdown extractor
    - Create ParseResult type: dict with 'strategy', 'tool_calls', 'confidence' keys
    - Create parse_response(text: str) -> ParseResult main function
    - First extract Python blocks with extract_python_blocks
    - Try strategies in order:
      1. Python AST on extracted blocks (confidence: 0.9)
      2. Key-value patterns on full text (confidence: 0.7)
      3. Inline code mentions on full text (confidence: 0.5)
    - Log which strategy succeeded/failed at DEBUG level
    - Return first successful result with strategy name
    - If all fail, return empty result with confidence 0
  </action>
  <verify>python -c "from src.perplexity_web_mcp.api.response_parser import parse_response; print(parse_response('SEARCH: test'))"</verify>
  <done>Parser orchestrator exists and applies strategies in fallback order</done>
</task>

<task type="auto">
  <name>Task 3: Add comprehensive error handling and logging</name>
  <files>src/perplexity_web_mcp/api/response_parser.py</files>
  <action>
    Enhance parser with proper error handling and informative logging.

    Updates to response_parser.py:
    - Wrap each strategy call in try/except block
    - Log exceptions at WARNING level with strategy name
    - Add timing measurements for each strategy
    - Log total parsing time at INFO level
    - Add normalize_tool_calls() helper to standardize output format
    - Ensure all strategies return consistent structure
    - Add docstrings explaining fallback chain behavior
    - Include example inputs/outputs in docstring
  </action>
  <verify>python -c "import logging; logging.basicConfig(level=logging.DEBUG); from src.perplexity_web_mcp.api.response_parser import parse_response; parse_response('invalid input')"</verify>
  <done>Parser has comprehensive error handling and logs strategy attempts</done>
</task>

</tasks>

<verification>
1. Parser imports all three strategies successfully
2. Markdown blocks extracted before Python parsing
3. Fallback chain executes in correct order
4. Failed strategies don't crash the parser
5. Successful parsing returns strategy name and confidence
</verification>

<success_criteria>
- parse_response() function exists and is importable
- Function tries AST strategy first on extracted Python blocks
- Falls back to key-value, then inline code strategies
- Returns ParseResult with strategy, tool_calls, and confidence
- All failures handled gracefully with logging
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-strategy-response-parser/02-04-SUMMARY.md`
</output>